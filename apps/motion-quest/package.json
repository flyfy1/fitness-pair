{
  "name": "@fitness-pair/motion-quest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:assets": "node scripts/prepare-assets.mjs",
    "dev": "npm run prepare:assets && vite --host 127.0.0.1 --port 5178 --strictPort",
    "build": "npm run prepare:assets && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 5178 --strictPort",
    "test": "node --test ../../tests/*.test.mjs",
    "test:browser": "playwright test"
  },
  "dependencies": {
    "@fitness-pair/contracts": "*",
    "@fitness-pair/pose-mediapipe": "*",
    "@fitness-pair/action-squat": "*",
    "@fitness-pair/game-forest": "*"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "vite": "8.3.0"
  }
}
